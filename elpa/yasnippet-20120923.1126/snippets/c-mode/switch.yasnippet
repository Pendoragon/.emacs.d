# -*- mode: snippet -*-
# name: switch(...) {case:...break;default:...break;}
# key: switch
# --
switch (${1:ch}) {
       case ${2:const}:
       ${3:a = b};
       break;
       ${4:default}:
       ${5:action};
       break;
}
